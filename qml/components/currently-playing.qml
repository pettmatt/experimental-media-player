import QtQuick 2.15
import QtQuick.Controls 2.15

Label {
    text: qsTr("Currently playing")
    font.pixelSize: 16
    // Optional styling
    color: Palette.primaryText  // Using the Palette singleton for text color
}
